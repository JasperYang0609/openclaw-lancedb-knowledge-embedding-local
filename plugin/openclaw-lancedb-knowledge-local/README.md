# OpenClaw Local Knowledge Search Plugin

This package is installed by `qwen-local integrate-openclaw`. It registers the read-only `local_knowledge_search` tool and only executes the installer-managed Qwen LanceDB project's JSON search command.

Do not install this directory by itself. The integration installer supplies and validates the project root, Node executable, project allowlist, service, schedule and rollback state.
